# Archive Folder - Old & Unused Files

This folder contains old versions, test files, and duplicates that were archived during cleanup.
Review these files and delete the entire `_archive` folder once confirmed they're not needed.

## Contents:

### `/stripe-lib-versions/`
- Old versions of stripe.ts library files (stripe-2.ts, stripe-3.ts)
- Not imported anywhere in the codebase

### `/stripe-api-old/`
- Old PHP API files for Stripe integration
- Versioned and test files replaced by current implementations

### `/cockpit3d-api-old/`
- Old versions of Cockpit3D data fetcher and image downloader PHP files

### `/components-old/`
- Backup and old versions of React components

### `/pages-old/`
- Old page versions (cart, admin, order-confirmation, checkout-orig)

### `/test-debug-files/`
- Files from /test-stuff/ directory used for debugging

### `/html-test-files/`
- Test HTML files used for debugging Stripe and other features

### `/css-old/`
- Old CSS file versions

## Safe to Delete?

Review each folder to ensure no critical code is archived, then delete the entire `_archive` folder.

**Date Archived:** $(date)
