<analysis>**original_problem_statement:**
The user's e-commerce store, built with Next.js and a PHP backend, has a critically non-functional shopping cart for custom products. The initial problem of missing product options and incorrect pricing evolved into a larger set of requirements focused on fixing the entire custom order lifecycle.

The primary goals are:
1.  **Fix Image Uploads:** A recurring and critical issue where customized product images are saved with an incorrect server file path, making them inaccessible to the frontend. The upload script must place images in a shared  directory, with separate subfolders for test () and production () environments. Each order must have its own unique subfolder.
2.  **Resolve Masked Image Failure:** The masked (final, engraved-look) image is consistently failing to upload, while the raw (original) image succeeds. This breaks the user experience as customers cannot see their final product in the cart or on the order confirmation.
3.  **Implement a Unified Order ID:** Create a single, consistent Order ID ( format) that is generated when a user starts customizing a product. This ID must be used for the image upload folder, cart data, Stripe session, Cockpit3D payload, and all email notifications to ensure data integrity.
4.  **Fix Environment-Specific Bugs:** The  environment was incorrectly using live production Stripe keys due to a flaw in the build process. The fix for this needs verification.
5.  **Create an Order Processing & Notification Flow:** Upon successful payment, a new backend process should trigger that sends the final order payload to an external Cockpit3D service and sends a comprehensive order confirmation email to .
6.  **Enhance Debugging:** Implement a comprehensive debug panel that displays all critical state information (unified order ID, cart data, environment variables, etc.) to help diagnose issues faster.
7.  **Address LocalStorage Bloat:** Prevent large base64 image data from being stored in  to avoid browser storage limits.

**User's preferred language**: English

**what currently exists?**
The application is a Next.js 14 e-commerce store with a standalone PHP backend for handling image uploads, Stripe payments, and order processing. The state is managed on the client-side. The user deploys the application by manually FTPing the build output ( folder) from their local MAMP server to a production server, which has been the source of numerous environment-related issues.

Recent significant additions include:
- A new Unified Order ID system ().
- A new comprehensive debug panel component ().
- A new central order processing endpoint () which handles Cockpit3D submission and triggers an enhanced email notification script.
- A completely rewritten PHP image upload script () designed to handle pathing correctly across test and production environments.

**Last working item**:
-   **Last item agent was working:** The agent was debugging why the **masked image** is not being uploaded, which results in it being missing from the cart and the final Cockpit3D order payload. The raw image uploads correctly, but the  (masked image URL) is absent from the final data. The agent also noticed that an **old, stale Order ID** was being reused across sessions, which is a critical data integrity problem.
-   **Status:** IN PROGRESS
-   **Agent Testing Done:** N
-   **Which testing method agent to use?** Frontend testing agent to simulate the full user flow: load product page, upload an image, save it in the editor, add to cart, and proceed to checkout. The agent must inspect the browser's console logs and network requests to validate the new logging and check for errors during the masked image upload.
-   **User Testing Done:** N

**All Pending/In progress Issue list**:
-   **Issue 1 (P0):** Masked image is not being uploaded or saved.
-   **Issue 2 (P1):** A stale, old Order ID is being reused across different user sessions.
-   **Issue 3 (P2):** The fix for incorrect Stripe keys in the  environment has not been verified by the user.
-   **Issue 4 (P3):** The fix for high LocalStorage usage has not been verified by the user.

**Issues Detail:**
-   **Issue 1: Masked image is not being uploaded or saved**
    -   **Attempted fixes:** The agent refactored the PHP upload script multiple times, unified the Order ID system, and added extensive logging to both the frontend (, ) and the PHP backend to trace the data flow. A user-facing alert was also added to notify if the masked upload fails.
    -   **Next debug checklist:**
        1.  Confirm the user has rebuilt (
> crystalkeepsakes@0.1.0 build:test
> node scripts/safe-build.js test


🔨 Starting SAFE build for TEST mode

📋 Step 1: Checking for locked files...

✅ No recently modified files found

📋 Step 2: Setting build environment...) and deployed the latest code to activate the new, detailed logging.
        2.  Request the **full browser developer console output** when the user saves an image. This is crucial to see the  data URL from the new logs in .
        3.  Request the **Network tab details** for the  call corresponding to the masked image, checking its payload and server response.
        4.  Investigate why  is  when an item is added to the cart, as this is the direct cause of the image not appearing.
    -   **Why fix this issue and what will be achieved with the fix?** This is the primary blocker for the entire custom product workflow. Fixing it will allow customers to see their final customized product in the cart and enable complete orders to be sent to production.
    -   **Status:** IN PROGRESS
    -   **Is recurring issue?** Y
    -   **Should Test frontend/backend/both after fix?** Both.

-   **Issue 2: A stale, old Order ID is being reused**
    -   **Attempted fixes:** None. The agent only just identified this from the user's last message.
    -   **Next debug checklist:**
        1.  Review  to ensure the  function is being called reliably after an order is completed.
        2.  Examine  to understand how the session is stored (e.g., ) and cleared.
        3.  Add logging to  to track when it finds an existing session versus creating a new one.
    -   **Why fix this issue and what will be achieved with the fix?** Prevents new orders from overwriting the image files of previous orders and ensures data integrity across the system. Each customization session must have a unique ID.
    -   **Status:** NOT STARTED
    -   **Is recurring issue?** Y
    -   **Should Test frontend/backend/both after fix?** Frontend.

**In progress Task List**:
None. All current work is focused on fixing the critical P0 and P1 issues.

**Upcoming and Future Tasks**
*   **Upcoming Tasks:**
    *   **(P1)** Verify the end-to-end order flow after fixing the masked image and stale ID issues.
    *   **(P2)** Implement the hidden faces option (1-10) in .
*   **Future Tasks:**
    *   **(P3)** Conduct a codebase audit to identify and remove unused files.
    *   **(P4)** Create a static Privacy Policy page.

**Completed work in this session**
-   **Unified Order ID System:** Created  and refactored the application to use a single, consistent order ID () from product customization through checkout.
-   **PHP Image Upload Path Fix:** Rewrote  to reliably save images to a shared  directory, with unique subfolders per order ID.
-   **Comprehensive Debug Overlay:** Implemented a new, detailed debug panel () to show all critical session and environment data.
-   **Email Notification System:** Created a new endpoint () that handles Cockpit3D submission and sends a detailed HTML email on successful order confirmation.

**Earlier issues found/mentioned but not fixed**
-   **Issue 1: Stripe keys in  environment.** A fix was implemented in the build script, but it requires user verification after a rebuild and deploy to confirm it's resolved.
-   **Issue 2: High LocalStorage usage.** A fix to strip base64 data before saving to localStorage was implemented in  but awaits user verification.

**Known issue recurrence from previous fork**
-   The incorrect image upload path has been a persistent, central theme. While the pathing logic is believed to be correct now, the failure of the masked image upload is a manifestation of this recurring problem area.

**Code Architecture**


**Key Technical Concepts**
-   **Frontend:** Next.js 14 (App Router), React, TypeScript, Tailwind CSS.
-   **Backend:** Standalone PHP scripts.
-   **State Management:** Client-side via , , and .
-   **Build Process:** A custom Node.js script () that manages environment variables for test vs. production builds.
-   **Deployment:** Manual FTP of the  directory.

**key DB schema**
-    (or ) stores the unified order session object, e.g., .

**changes in tech stack**
-   None.

**All files of reference**
-   **Core of current bug:**
    -   : Orchestrates the image upload via .
    -   : Generates the masked image data.
    -   : Receives and saves the uploaded image files.
-   **Stale ID issue:**
    -   : Source of truth for order IDs.
    -   : Location where the session should be cleared.
-   **New Features:**
    -   : The new debug panel.
    -   : The new order processing endpoint.

**Critical Info for New Agent**
-   Your absolute first priority is to resolve the **failing masked image upload (P0)**. Do not attempt other changes until you get diagnostic data for this.
-   Your second priority is to fix the **stale Order ID issue (P1)**. This is a critical data integrity bug.
-   **You MUST get diagnostic data from the user.** The previous agent added extensive logging. Instruct the user to rebuild and deploy the latest code, then capture and share the browser's developer console output and network tab details during an image save attempt. You cannot debug this blind.
-   Reinforce the user's manual deployment workflow: any code change requires 
> crystalkeepsakes@0.1.0 build:test
> node scripts/safe-build.js test


🔨 Starting SAFE build for TEST mode

📋 Step 1: Checking for locked files...

✅ No recently modified files found

📋 Step 2: Setting build environment... followed by FTPing the **entire  directory** to their server.

**Last 10 User Messages and any pending HUMAN messages**
-   User is frustrated that the masked image is missing from the final order payload and not showing in the cart debug panel, stating it was working.
-   User provided a Cockpit3D JSON payload showing  exists but  (the masked image) is missing.
-   User provided another screenshot showing the cart is not displaying the masked image.
-   User confirmed the email address for notifications is .
-   User reported an ID mismatch between the image folder (), the test order ID (), and the Stripe session ID (), which was a critical clue that led to the creation of the unified ID system.

**Project Health Check:**
-   **Broken:** The core user flow for custom products is critically broken because the masked image is not being saved or displayed.
-   **Mocked:** None.

**3rd Party Integrations**
-   **Stripe:** For payment processing.
-   **Cockpit3D:** An external service that receives the final order payload.

**Testing status**
-   **Testing agent used after significant changes:** NO
-   **Troubleshoot agent used after agent stuck in loop:** NO
-   **Test files created:** None.
-   **Known regressions:** The masked image upload was allegedly working at some point but is now broken again.

**Credentials to test flow:**
-   N/A. All testing must be performed by the user in their own environments.

**What agent forgot to execute**
-   The agent correctly identified the stale Order ID issue as a major problem but has not yet attempted a fix. This must be prioritized right after getting debug info for the masked image failure.
-   Verification for the Stripe key and LocalStorage fixes is still pending from the user.</analysis>
